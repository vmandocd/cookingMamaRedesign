<% layout('layout') -%>

<div id="midContainer" class="container">

  <div id="pplBtn" class="container">
    <h1 style="padding-top:50px;">Feed</h1>
    <a href="/publicFeed"><button id="testjs" class="btn btn-primary">Public</button></a>
    <a href="/profile"><button id="testjs" class="btn btn-primary active">You</button></a>
  </div>

  <div class="profileRedesign">
     <div class="container">
          <div class="row">

            <% for(var i = 0; i < user.history.length; i++) { %>

                  <div class="col-xs-6" style="opacity:0.9;">
                      <div class="thumbnail">
                          <img src="<%= user[i].history[j].image %>">
                          <div class="caption">
                              <h3><%= user.profile.name %></a> prepared <a href="/product/<%= user.history[i].item %>"><%= user.history[i].name %></a></h3>
                          </div>
                      </div>
                  </div>

            <% } %>

          </div>
      </div>
  </div>

</div>
